prime:
  activerecord: true
  default_connection: 'test'
  cache:
    query:
      pool: 'custom.cache'
  connections:
    test:
      url: 'sqlite::memory:'

  migration:
    connection: 'test'
    path: '%kernel.project_dir%/src/Migration'
